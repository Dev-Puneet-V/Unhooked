# Profile

User profile, stats, settings entry points that are **profile-centric**.

Cross-cutting UI still belongs in `src/shared/`; navigation shells can live here or in a future `src/app/` module.
