# Quiz

Daily quiz gameplay: questions, timer, scoring, results.

Suggested layout:

- `screens/` — play flow, results, history
- `components/` — only quiz-specific UI (shared buttons/cards go in `src/shared/components/`)
- `hooks/` — quiz state, submission
